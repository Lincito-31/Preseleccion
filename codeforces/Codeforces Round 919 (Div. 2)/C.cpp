#include <bits/stdc++.h>
using namespace std;
int t,n;
int main(){
    scanf("%d",&t);
    while(t--){
        scanf("%d",&n);
        // caso if k=n;
        for(int i=2;i*i<n;i++){
            if(n%i==0){
                // check for i;
                
            }
        }
        // caso if k*k=n;
    }
}