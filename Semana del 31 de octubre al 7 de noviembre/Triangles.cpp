#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;
int n,q,t,a,b;
int main(){
    scanf("%d%d",&n,&q);
    vi nums(n);
    for(int i=0;i<n;i++){
        scanf("%d",&nums[i]);
    }
    while(q--){
        scanf("%d%d%d",&t,&a,&b);
        if(t){
            //query
        }else{
            //update
        }
    }
}