#include <bits/stdc++.h>
using namespace std;
int main(){
    mt19937 rng(chrono::steady_clock().now().time_since_epoch().count());
    vector<string> st={"en","eo","jbo",""};
    int ant=0;
    for(int i=0;i<1000;i++){
        uniform_int_distribution<int> (0,4);
        
    }
}